# Yahoo! Cloud System Benchmark
# Workload for measuring leveldb with tsx
#
# load 2M records
# Then we perform 10M random inserts, and reads
# 30% inserts, and 70% are reads


recordcount=2000000
operationcount=10000000
workload=com.yahoo.ycsb.workloads.CoreWorkload

readallfields=true

readproportion=0.7
updateproportion=0
scanproportion=0
insertproportion=0.3

requestdistribution=zipfian

threadcount=10